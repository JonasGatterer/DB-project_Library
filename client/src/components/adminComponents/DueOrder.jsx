import React from "react";

const DueOrder = () => {
    return(
        <div>Check all orders which have passed the Due Date and the Publications that have not yet returned.</div>
    )
}

export default DueOrder;